import { fontSize, space } from '../../helpers/theme'
import styled from 'styled-components'

export const H_Heading = styled.h2`
  font-family: 'Raleway', sans-serif;
  font-size: ${fontSize.big};
  padding-bottom: ${space.small};
`
