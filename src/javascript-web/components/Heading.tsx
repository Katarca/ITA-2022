import { styles } from '../../helpers/theme'
import styled from 'styled-components'

export const H_Heading = styled.h2`
  font-family: 'Raleway', sans-serif;
  font-size: ${styles.fontSize.lg};
  padding-bottom: ${styles.spacing.sm};
`
